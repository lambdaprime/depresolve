/*************************************************
Requires gradle 7.2

gradle clean build

**************************************************/

allprojects {
    repositories {
        mavenCentral()
    }
}

ext {
    releaseFileName = "${projectName}-v${version}.zip"
}